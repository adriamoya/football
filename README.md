# Football Analysis

### Installation & Setup

```shell
mkdir football && cd football
virtualenv .
source bin/activate
sudo pip install scrapy
```

or

```shell
mkdir football && cd football
virtualenv .
source bin/activate
sudo pip install -r requirements.txt
```
